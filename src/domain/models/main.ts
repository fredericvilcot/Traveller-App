export type TLocation = {
    lat: number
    long: number
}

export type TCurrency = {
    id: string
    name: string
    unitSymbols: string[]
    isoCode: string
}

export type Tlanguage = {
    alpha2Code: string
    id: string
    name: string
}
